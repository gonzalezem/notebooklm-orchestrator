# Implementation checklist — {{intent}} / {{query}}

Create an implementation checklist for "{{query}}" based on the sources.

Format as ordered steps. Flag any steps where sources disagree on approach.
